%table
  %thead
    %tr
      - ["Title", "Start Date", "End Date", "Address"].each do |heading|
        %th= heading
  %tbody
    - active_courses.each do |course|
      = render 'course', course: course
